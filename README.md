#NG Book 2 Concepts

##How to set up

run:

`npm install`

update:

`packages path and system.import path in index.html to point to concept directory`

run:

`npm start`


##Current status
Chapter 1 complete